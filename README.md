# Frontend — Concept Clarity Prototype

Requirements: Node.js (16+) and npm or yarn.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173 and use the role buttons to upload lectures (professor) or take quizzes (student). The frontend expects the backend API at `http://127.0.0.1:8000`.
